extends ../layout

block header
    - cur = "consultas"

block content
    h1 #{title}
    form(method='POST', action='/consultas/1')
        small
            i Exemplo
        pre
            | select count(*) from utilizacao u 
            |   where u.CPF_AUDIOVISU = '111.111.111-44' 
            |   AND extract('MONTH' from u.DATA_UTI) = '04'

        #form-group
            lable CNPJ Audio-Visual:
            input.form-control(name='cnpj_audiovisu', type='text')
        #form-group
            lable Mês em que utilizou:
            input.form-control(name='month', type='number', min='1', max='12', step='1')
        br
        button.btn.btn-info(type='submit')
            span.btn-label
                i.fa.fa-search
                |   Consulta