extends ../layout

block header
    - cur = "empresas_artisticas"

block content
    h1 #{title}
    p
        a(href='/empresas/artisticas/add')
            button.btn.btn-labeled.btn-success(type='button')
                span.btn-label
                    i.fa.fa-plus
                    |   Adicionar Empresa Artística
    if !err
        .table-responsive
            table.table.table-hover
                thead.thead-dark
                    tr
                        - var keys = Object.keys(empresas[0])
                        - keys.push(''/*, ''*/)
                        each val in keys
                            if val == "data_nasc"
                            th= val
                tbody
                    each empresa, i in empresas
                        tr
                            each key in empresa
                                td= key
                            td
                                a(href='/empresas/artisticas/update/' + empresa.cnpj_art)
                                    button.btn.btn-labeled.btn-warning(type='button')
                                        span.btn-label
                                            i.fa.fa-edit